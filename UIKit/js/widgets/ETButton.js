var ETButton = Class.create(ETWidget, {	/*			@name Pushbutton Widget		@author Gray		@param parent, properties			A simple, button widget	*/		initialize: function($super, parent, properties) {			$super(parent, properties);				// Button Text		if (properties.text) {					this.text = properties.text;				} else {					this.text = "undefined!";				}				this.changeType('span');		this.setText(this.text); // Set the button text				// Apply the button class		this.getElement().addClassName("ETButton");				// Setup handlers		this.getElement().observe('mousedown', this.onClick);		//this.getElement().observe('mouseover', this.mouseOver);		this.getElement().observe('mouseup', this.onOff);				this.parent.addElement(this); // add to parent		},		onClick: function() {			// Handle button clicking visually. This is not handler specific!!				this.addClassName("ETButtonSelected");		},		onOff: function() {			// Handle button clicking visually. This is not handler specific!!				this.removeClassName("ETButtonSelected");		},		setText: function(text) {			/*					@name setText			@param text						Sets the text of this label				*/				this.getElement().update(text);		}});