var ETWindowContent = Class.create(ETContainer, {	/*			@name Window Content Widget		@author Gray		@param parent, properties			This is for basic window content.	*/		initialize: function($super, parent, properties) {			properties.selectable = true;			$super(parent, properties);			this.getElement().addClassName("ETWindowContent");		this.parent.addElement(this);		this.getElement().setOpacity(1);	}	});